# skills-directory

Shared directory of reusable assets for coding agents across tools and machines.

## Structure

- `slash-commands/` - reusable slash commands
- `docs/plans/` - planning docs linked to implementation commits

## Included

- `slash-commands/log_error` - cross-agent log-error command prompt package for Claude Code, Codex, and OpenCode.
